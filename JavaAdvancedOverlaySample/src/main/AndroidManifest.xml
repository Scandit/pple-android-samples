<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_pple_sample_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_pple_sample_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DebugApp">
        <activity
            android:name="com.scandit.shelf.javaadvancedoverlaysample.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
